<?xml version="1.0" encoding="utf-8"?>
<!-- Default / fallback: no cleartext. The `dev` flavor overrides with LAN/emulator cleartext. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
